# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ~/FALL-DETECTION/dataset # dataset root dir
train: ~/FALL-DETECTION/dataset  # train images (relative to 'path') 4 images
val: ~/FALL-DETECTION/valid # val images (relative to 'path') 4 images
test:  ~/FALL-DETECTION/train # test images (optional)

# Classes (80 COCO classes)
# Classes
names:
  - fall
  - no_fall
